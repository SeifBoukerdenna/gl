{
    "_description": "Oracle LAZY REVERSE — same logic as oracle_lazy (queue signal, wait 10s, fire if persistent) but FIRES THE OPPOSITE DIRECTION at fire time. Contrarian sanity check: if oracle is bleeding in current regime, this should profit. If both lose, the regime is hostile to any directional bet.",
    "ARCHITECTURE": "oracle_lazy_reverse",
    "ONE_TRADE_PER_WINDOW": false,
    "BASE_TRADE_DOLLARS": 200,
    "MAX_SHARES": 600,
    "MAX_RISK_PCT": 0.99,
    "STARTING_BANKROLL": 999999,
    "OR_BASE_DOLLARS": 200,
    "OR_PHASE1_MIN_EDGE": 0.08,
    "OR_PHASE2_MIN_EDGE": 0.05,
    "OR_MAX_ENTRY": 0.88,
    "OR_MIN_ENTRY": 0.10,
    "OR_COOLDOWN_SEC": 10,
    "LAZY_WAIT_SEC": 10,
    "LAZY_MAX_DEGRADATION": 0.02,
    "EDGE_TABLE_FILTER": false,
    "CHOP_FILTER": false,
    "LOSS_COOLDOWN": false,
    "SIGNAL_CHECK_MS": 50
}
