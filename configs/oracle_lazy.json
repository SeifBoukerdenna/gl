{
    "_description": "Oracle LAZY V2 — vol-adaptive direction-stable persistence. Instead of a fixed 10s wait, queues a signal and requires N consecutive same-side 1s ticks after a vol-proportional minimum wait. Max wait 25s. Edge must stay >= 80% of original. Designed to filter chop (BTC oscillates across strike → never accumulates stable ticks → never fires).",
    "ARCHITECTURE": "oracle_lazy",
    "ONE_TRADE_PER_WINDOW": false,
    "BASE_TRADE_DOLLARS": 200,
    "MAX_SHARES": 600,
    "MAX_RISK_PCT": 0.99,
    "STARTING_BANKROLL": 999999,
    "OR_BASE_DOLLARS": 200,
    "OR_PHASE1_MIN_EDGE": 0.08,
    "OR_PHASE2_MIN_EDGE": 0.05,
    "OR_MAX_ENTRY": 0.88,
    "OR_MIN_ENTRY": 0.10,
    "OR_COOLDOWN_SEC": 10,
    "LAZY_MIN_WAIT_FLOOR_SEC": 3,
    "LAZY_MIN_WAIT_CEIL_SEC": 12,
    "LAZY_VOL_SCALE": 0.1,
    "LAZY_STABLE_TICKS": 3,
    "LAZY_MAX_WAIT_SEC": 25,
    "LAZY_EDGE_DEGRADATION_MAX": 0.20,
    "EDGE_TABLE_FILTER": false,
    "CHOP_FILTER": false,
    "LOSS_COOLDOWN": false,
    "SIGNAL_CHECK_MS": 50
}
